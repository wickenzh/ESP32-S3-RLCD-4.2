# ESP32-S3 RLCD 天气时钟

这是一个基于 Waveshare ESP32-S3-RLCD-4.2 开发板的低功耗天气时钟项目。当前主固件工程仍保留在 `RLCD_CLOCK/` 目录中，仓库根目录用于放置项目说明、硬件资料、示例代码和后续会用到的资源文件。

## 硬件与技术栈

- 主控：ESP32-S3，16 MB Flash，带 PSRAM。
- 屏幕：4.2 英寸 RLCD 反射式 LCD。
- UI：LVGL 8，支持 `RLCD_CLOCK/simulator/` 下的 SDL 本地预览。
- RTC：PCF85063，通过 I2C 通信。
- 本地传感器：SHTC3，通过 I2C 读取温度和湿度。
- 电池检测：ADC1 Channel 3 采样，按 3 倍分压换算电池电压。
- 天气服务：和风天气 QWeather，使用 API Key 认证，请求头为 `X-QW-Api-Key`。
- 时间同步：NTP，服务器包括 `pool.ntp.org`、`ntp.aliyun.com`、`time.windows.com`。

## 固件功能逻辑

- 开机后显示启动页，包含项目名称、版本号、启动状态和 GIF 抽帧动画。
- 如果已经保存 WiFi 信息，开机后会连接 WiFi，并进行一次 NTP 时间同步和天气同步。
- 如果没有 WiFi 信息，设备会进入配网模式，AP 名称格式为 `WeatherClock-XXXX`；每次从非配网状态进入配网模式时，会播放一次 WiFi 配网提示音。
- 配网方式：连接设备 AP 后打开 `192.168.4.1`，输入 WiFi 名称、WiFi 密码和 QWeather API Key。
- NTP 同步规则：开机同步一次，之后每天 0 点同步一次。
- 天气同步规则：开机或首次配网成功后同步一次；正常时段按每小时整点同步，22:00 到次日 06:00 改为每 2 小时整点同步一次。
- 天气预警同步规则：与实时天气共用 API Key 和同步周期；低电量极简模式下跳过预警请求。
- 本地温湿度读取规则：系统时间可信后正常时段按每分钟整点读取一次，22:00 到次日 06:00 改为每 2 分钟整点读取一次；同时保留最近 2 小时数据，用滚动平均值判断温度和湿度趋势并显示上下箭头。
- 电池读取规则：正常时段按每整 5 分钟读取一次，22:00 到次日 06:00 改为每整 10 分钟读取一次。
- 低功耗策略：非联网同步时间窗口会主动关闭 WiFi；CPU 降频并启用 light sleep，同时保持 RLCD 屏幕显示。

## 主界面显示

- 时间使用断码屏风格的大号数字显示，小时和分钟最大，秒显示较小。
- 日期显示在右上角，格式为 `YYYY/MM/DD / 星期几`。
- 电池图标显示在左上角，内部 5 个小格，每格代表约 20% 电量。
- 城市、网络天气、本地温度和本地湿度显示在主界面下方区域。
- 本地温度和湿度会分别显示趋势箭头；最近 2 小时滚动平均值上升显示上箭头，下降显示下箭头，数据不足或无明显变化时不显示箭头。
- 当天气 API 返回有效预警时，顶部空白区域会显示黑底白字的椭圆预警条，并按秒闪烁；无预警时该区域保持空白。
- 当电池电量低于 10% 时，进入低电量极简模式：隐藏秒、小进度条、天气、本地温湿度和 GIF 动画，只保留分钟级时间、日期、电池和底部中间的低电量提醒图标；电量恢复到 13% 以上后返回正常工作界面。
- 正常工作界面不显示 `WiFi OFF`、`AP OFF`、`NTP OK` 等状态文字，避免干扰主要信息。
- 进入配网模式时，底部三等分信息栏会临时隐藏，改为显示设备 AP、AP 密码、门户地址、当前保存的 WiFi 和 STA IP 状态；配网完成后恢复正常三格信息。
- 中文 UI 使用生成的 LVGL 字体，当前字体来源为 `Hiragino Sans GB W6 Bold`，尺寸为 16 px，1 bpp，适配单色 RLCD 显示链路。
- 天气图标使用 QWeather 图标字体转换后的 LVGL 字体。

## 页面预览

页面预览图由 `scripts/generate_previews.sh` 通过 LVGL SDL 模拟器生成。只有 UI 页面发生变化时才需要更新这些图片；完整发版脚本会在 SDL 编译后自动刷新预览。

启动页：

![启动页](assets/previews/weather_clock_boot.png)

主工作界面：

![主工作界面](assets/previews/weather_clock_main.png)

天气预警状态：

![天气预警状态](assets/previews/weather_clock_alert.png)

低电量极简模式：

![低电量极简模式](assets/previews/weather_clock_low_battery.png)

设置页面：

![设置页面](assets/previews/weather_clock_settings.png)

配网状态界面：

![配网状态界面](assets/previews/weather_clock_setup.png)

## KEY 设置页面

KEY 键位于 GPIO18，低电平按下。当前设置页面逻辑如下：

- 单击 KEY：进入设置页面。
- 设置页面 5 秒无操作：自动返回正常工作界面。
- 设置页面内短按 KEY：循环选择下一项。
- 设置页面内短按 BOOT：确认当前选项。

设置页面包含 5 个项目：

- `整点提醒`：切换 ON/OFF，并保存到 NVS。开启时会立即播放一次提示音用于确认；每天 `07:00` 到 `22:00` 的整点播放一次短提示音，提示音通过 ES8311/I2S 播放，播放结束后关闭音频输出；处于低电量极简模式时暂停播放，电量恢复后继续按用户设置生效。
- `同步时间`：立即请求一次 NTP 同步，由后台网络任务打开 WiFi 并完成同步。
- `同步天气`：立即请求一次 QWeather 实时天气同步，由后台网络任务处理。
- `恢复出厂设置`：清除已保存的 WiFi 与 API Key，并进入配网模式。
- `关于本机`：进入 System Info 信息页，查看 NTP、WiFi、天气查询、电池和版本信息。

## System Info 信息页

System Info 信息页从设置页面的 `关于本机` 进入，只使用英文和数字，显示内容包括：

- 上一次 NTP 同步时间。
- 当前保存的 WiFi SSID。
- 上一次天气 API 查询时间。
- 电池百分比和电池电压。
- 当前软件版本号。

## 项目目录结构

- `RLCD_CLOCK/`：当前主工程目录，包含 ESP-IDF 固件、组件、配置文件和 SDL 预览工程。
- `RLCD_CLOCK/main/`：天气时钟应用主体代码、字体资源、天气图标字体和开机动画抽帧资源。
- `RLCD_CLOCK/components/`：板级 BSP、显示驱动、I2C、传感器和厂商依赖组件。
- `RLCD_CLOCK/components/ExternLib/codec_board` 与 `esp_codec_dev`：用于 ES8311 整点提醒提示音播放的本地音频组件。
- `RLCD_CLOCK/managed_components/`：ESP-IDF 管理的第三方组件，目前主要是 LVGL。
- `RLCD_CLOCK/simulator/`：LVGL SDL 本地预览工程，用于在烧录前检查界面效果。
- `docs/`：原理图、芯片手册、传感器资料和其他硬件文档。
- `examples/esp-idf/`：厂商提供的 ESP-IDF 示例工程，作为硬件驱动和外设逻辑参考。
- `assets/weather-icons/`：和风天气图标 SVG、字体和源文件。
- `assets/gif_video/`：后续 UI 或媒体功能可能会使用的 GIF 和音频资源。
- `scripts/`：项目辅助脚本，目前包含 Gitea Release 发布脚本。
- `README.md`：项目说明、功能逻辑、目录结构和版本记录。

## 发布与下载

每个正式测试版本都会同步 Git 标签，并在 Gitea Release 中上传编译好的固件产物，方便直接下载测试。

Release 附件包括：

- `weather_clock_vX.X.X.bin`：App 固件。
- `weather_clock_vX.X.X_flash_package.zip`：包含 bootloader、分区表、App 固件、`flash_args` 和简短烧录说明。
- `weather_clock_vX.X.X_merged.bin`：合并后的完整镜像，适合需要整包写入时使用。

发布附件上传脚本为 `scripts/publish_gitea_release.sh`。完整发版脚本为 `scripts/release_version.sh`，会依次执行固件编译、SDL 模拟器编译、代码检查、提交、打标签、推送和上传 Release 附件。脚本不会保存账号密码，需要运行时提供 `GITEA_TOKEN`，或者提供 `GITEA_USER` 和 `GITEA_PASSWORD`。

## 版本记录

- `v1.1.2`：低电量极简模式下暂停整点提醒播放，包括整点自动提醒和设置页开启时的试听音；用户的整点提醒开关状态仍保留，电量恢复后自动继续按原设置生效。
- `v1.1.1`：修复在设置页快速反复开关 `整点提醒` 时可能出现的 I2S 通道重复关闭报错；低电量极简模式进入阈值从 5% 提升到 10%，恢复阈值同步调整为 13%，减少低电量边界反复切换。
- `v1.1.0`：继续做系统稳定性和功耗优化。修复音频播放结束后可能重复关闭 I2S 通道导致的 `i2s_channel_disable` 报错，并将 TDM 默认 MCLK 调整为 384；音频打开失败会安全返回；网络同步任务空闲等待从最多 1 分钟延长到 5 分钟，减少后台无意义唤醒。
- `v1.0.20`：系统稳定性和功耗优化。运行期 WiFi、配网 HTTP、NVS 保存等非致命失败改为安全返回并记录日志；联网和音频播放期间增加短时 PM 锁；音频播放状态改为临界区保护；优化 GIF 秒级刷新和 LVGL flush 裁剪，减少无效写屏。
- `v1.0.19`：设置页中的 `整点报时` 改名为 `整点提醒`；KEY 键进入设置页由长按 2 秒改为单击进入，设置页内仍保持 KEY 选择、BOOT 确认。
- `v1.0.18`：修复配网提示音播放时机和卡顿问题。首次未配网启动时，提示音改为在启动页结束、主界面显示稳定后播放；音频写入块增大并提高短音频播放任务优先级，减少播放时的断续感。
- `v1.0.17`：新增配网模式提示音。项目中的 `wifi.mp3` 已转换为固件内置 PCM，设备每次从非配网状态进入配网模式时播放一次，覆盖首次未配网启动和恢复出厂设置进入配网等路径。
- `v1.0.16`：调整周期性数据更新策略。网络天气和天气预警改为按整点同步，正常时段每小时一次，22:00 到次日 06:00 每 2 小时一次；本地温湿度正常时段每分钟整点读取，夜间每 2 分钟整点读取；电池正常时段每整 5 分钟读取，夜间每整 10 分钟读取。
- `v1.0.15`：根据音频诊断结果，将整点报时正式固定为 `MR` 偏右混合声源并输出到 `slot0`；移除设置页中的临时音频测试项，其他音频参数暂不继续调整。
- `v1.0.14`：第二轮音频诊断固件，确认单扬声器取用 `slot0` 后，将所有测试统一输出到 `slot0`，分别测试左声道、右声道、左右混合和偏右混合；同时为 4-slot TDM 播放恢复 384 MCLK，消除 MCLK 过低警告。
